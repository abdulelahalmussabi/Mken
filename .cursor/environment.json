{
  "name": "Mken Platform (mken.live)",
  "install": "npm install",
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8080 --bind 0.0.0.0",
      "description": "Static dev server for the Mken platform. Open http://localhost:8080 (index.html storefront, landing.html, book.html, admin.html). The /api/* Vercel serverless routes are not served here and require Supabase secrets."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ],
  "chromeExecutablePath": "/usr/bin/google-chrome-stable"
}
